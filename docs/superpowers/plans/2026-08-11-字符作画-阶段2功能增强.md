# 字符作画 · 阶段 2 功能增强 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在阶段 1（文档式骨架 + 悬浮素材面板）基础上，为字符作画页补齐 6 项编辑能力：画布缩放、旋转、透明度、图层面板、方向键微调、多选（移动+删除），并扩展快照数据模型（rotation/opacity + 旧数据兼容）。

**Architecture:** 全部在 `字符作画/字符作画.js` 内扩展（vanilla JS，不改共享 CSS/lib）。缩放 = 视图变换 `translate(x,y) scale(z)`；旋转/透明度 = 元素 `transform: rotate()` / `opacity`；多选 = 选中集合 `Set` 重构；图层面板 = 悬浮面板底部折叠区。快照 `captureSnapshot/applySnapshot` 扩展 `rotation/opacity` 字段，旧 localStorage 数据自动补默认值。

**Tech Stack:** Vanilla JS + 共享 CSS（common.css 变量）。不引入任何库。

## Global Constraints

- **不动画布内容渲染方式**：字符仍为 HTML div，emoji 走原生字体渲染（阶段 1 决策，不经 canvas 位图）。
- **保留全部元素 ID**（阶段 1 已确认）：新增控件 ID 见各任务；既有 ID（sceneCanvas/canvasContainer/rightPanel/libraryPanel/propsPanel/各按钮滑块）不得改名。
- **阶段 1 悬浮面板布局不变**：属性面板选中时叠加在素材库上方；图层面板**叠在素材库下方**（本次新增，可折叠）。
- **多选仅支持移动 + 删除**：批量改字号/颜色/旋转不做（YAGNI，变换中心复杂）。
- **数据兼容**：`localStorage` 键 `char_scene_data` 不变；旧场景缺 `rotation/opacity` 自动补默认（0 / 1）。
- 不修改 `lib/`；不新建 package.json/node_modules。
- 快照/撤销重做机制（`captureSnapshot/pushHistory/undo/redo`）沿用，批量操作合并为单快照。

---
---

### Task 1: 数据模型扩展（rotation / opacity 字段 + 旧数据兼容）

**Files:**
- Modify: `字符作画/字符作画.js`

**Interfaces:**
- Consumes: 现有 `captureSnapshot()` / `applySnapshot()` / `createCharacterDiv()`
- Produces: 元素新增 `data-rotation`、`data-opacity` 属性；快照元素新增 `rotation`、`opacity` 字段（默认 0 / 1）

**Goal:** 快照能存取 rotation/opacity，旧数据无字段时用默认值，为 Task 3 打底。本任务不暴露任何 UI。

- [ ] **Step 1: `createCharacterDiv` 初始化新属性**

在现有 `createCharacterDiv(character, fontSize, repeat, color, customZ, left, top)` 内，设置 `div.style.opacity = 1;` 与 `div.setAttribute('data-opacity', 1);` `div.setAttribute('data-rotation', 0);`（默认无旋转/全透明）。

- [ ] **Step 2: `captureSnapshot` 追加字段**

在 `items.forEach` 的 push 对象中追加：
```js
rotation: parseFloat(el.getAttribute('data-rotation')) || 0,
opacity: parseFloat(el.getAttribute('data-opacity')) || 1,
```

- [ ] **Step 3: `applySnapshot` 恢复字段**

在 `snapshot.elements.forEach` 创建 div 后追加：
```js
if (e.rotation) { div.style.transform = `rotate(${e.rotation}deg)`; div.setAttribute('data-rotation', e.rotation); }
if (e.opacity != null && e.opacity !== 1) { div.style.opacity = e.opacity; div.setAttribute('data-opacity', e.opacity); }
```
（旧数据 `e.rotation`/`e.opacity` 为 undefined → 走默认分支不设置，兼容。）

- [ ] **Step 4: 验证**

- 无 UI 变化；现有功能（添加/保存/加载/撤销重做）不回归。
- 兼容测试：手动构造一份不含 rotation/opacity 的 `char_scene_data` 存进 localStorage，加载不报错，元素正常显示。
- 自闭环：`node --check 字符作画.js`；grep 确认三处改动就位。

- [ ] **Step 5: Commit**

```bash
git add "字符作画/字符作画.js"
git commit -m "增强：字符作画页快照数据模型扩展 rotation/opacity 字段"
```

---
---

### Task 2: 画布缩放（视图缩放）

**Files:**
- Modify: `字符作画/字符作画.html`（工具栏加缩放控件）
- Modify: `字符作画/字符作画.js`（zoom 状态 + 视图变换 + Ctrl+滚轮 + drop 换算）
- Modify: `字符作画/字符作画.css`（缩放显示样式，如需要）

**Interfaces:**
- Consumes: `sceneTransform`、`clampTransform()`、`addSymbolToCanvas(char, x, y)`（画布相对坐标）
- Produces: `let zoom`、`applyViewTransform()`、`setZoom(z)`、`updateZoomUI()`；元素 ID `zoomOutBtn/zoomDisplay/zoomInBtn/zoomResetBtn`

**Goal:** Ctrl+滚轮与工具栏按钮缩放视图 0.5~2.5；只改视图不改元素坐标；drop/粘贴落点按 zoom 换算。

- [ ] **Step 1: 工具栏加缩放控件**

`字符作画.html` 的 `#topToolbar` 末尾追加一个 `.one.group`：
```html
<div class="one group">
    <button class="btn" id="zoomOutBtn" title="缩小 (Ctrl+滚轮)">🔍−</button>
    <span class="zoom-display" id="zoomDisplay">100%</span>
    <button class="btn" id="zoomInBtn" title="放大 (Ctrl+滚轮)">🔍+</button>
    <button class="btn" id="zoomResetBtn" title="重置为 100%">重置</button>
</div>
```

- [ ] **Step 2: JS 加 zoom 状态与视图变换**

在 `sceneTransform` 声明旁加：
```js
let zoom = 1;
const MIN_ZOOM = 0.5, MAX_ZOOM = 2.5, ZOOM_STEP = 0.1;
```
新增（放在 `clampTransform` 前）：
```js
function applyViewTransform() {
    canvasElement.style.transform = `translate(${sceneTransform.x}px, ${sceneTransform.y}px) scale(${zoom})`;
}
function setZoom(z) {
    zoom = Math.min(MAX_ZOOM, Math.max(MIN_ZOOM, Math.round(z * 10) / 10));
    clampTransform();
    applyViewTransform();
    updateZoomUI();
}
function updateZoomUI() {
    const d = document.getElementById('zoomDisplay');
    if (d) d.textContent = Math.round(zoom * 100) + '%';
    const o = document.getElementById('zoomOutBtn'), i = document.getElementById('zoomInBtn');
    if (o) o.disabled = zoom <= MIN_ZOOM;
    if (i) i.disabled = zoom >= MAX_ZOOM;
}
```

**把全文件所有 `canvasElement.style.transform = translate(...)` 直接赋值替换为 `applyViewTransform();`**（现存在于 `applySnapshot`、`clampTransform`、`initCanvasPan` 的 mousemove/mouseup）。`newScene` 里重置 transform 的 `translate(0px,0px)` 改 `sceneTransform={x:0,y:0}; zoom=1; applyViewTransform(); updateZoomUI();`。

- [ ] **Step 3: Ctrl+滚轮 + 按钮绑定**

在 `init()` 中新增：
```js
container.addEventListener('wheel', (e) => {
    if (e.ctrlKey) { e.preventDefault(); setZoom(zoom + (e.deltaY < 0 ? ZOOM_STEP : -ZOOM_STEP)); }
}, { passive: false });
document.getElementById('zoomInBtn').onclick = () => setZoom(zoom + ZOOM_STEP);
document.getElementById('zoomOutBtn').onclick = () => setZoom(zoom - ZOOM_STEP);
document.getElementById('zoomResetBtn').onclick = () => setZoom(1);
updateZoomUI();
```

- [ ] **Step 4: drop/粘贴坐标按 zoom 换算**

`initDrop` 的 drop 处理器：
```js
const r = canvasElement.getBoundingClientRect();
addSymbolToCanvas(char, (e.clientX - r.left) / zoom, (e.clientY - r.top) / zoom, true);
```
`initPaste` 已用画布居中坐标，无需改（居中不随 zoom 变）。

- [ ] **Step 5: 验证**

- 缩放按钮/Ctrl+滚轮在 0.5~2.5 生效；`zoomDisplay` 实时更新；边界按钮 disabled。
- 缩放到 200% 后拖放一个素材，元素落在光标对应画布位置（用 getBoundingClientRect 反算校验）。
- 缩放后平移、撤销重做、resize 手柄仍正常（手柄用 getBoundingClientRect，自动含变换）。
- `clampTransform` 用实测 rect，缩放后平移边界仍限制在合理范围。

- [ ] **Step 6: Commit**

```bash
git add "字符作画/字符作画.html" "字符作画/字符作画.js" "字符作画/字符作画.css"
git commit -m "增强：字符作画页画布视图缩放（Ctrl+滚轮 + 工具栏）"
```

---
---

### Task 3: 旋转 + 透明度（属性面板）

**Files:**
- Modify: `字符作画/字符作画.html`（props 面板加旋转/透明度滑块）
- Modify: `字符作画/字符作画.js`（updatePropsPanelWithElement 接线）

**Interfaces:**
- Consumes: Task 1 的 `data-rotation`/`data-opacity`；现有 `updatePropsPanelWithElement(el)`
- Produces: 元素 ID `rotationSlider/rotationValueDisplay/opacitySlider/opacityValueDisplay`

**Goal:** 属性面板可调旋转（-180~180）与透明度（0~100%），随快照存取。

- [ ] **Step 1: props 面板加控件**

`字符作画.html` 的 `#selectionControls` 内、字号滑块行之后加：
```html
<div class="slider-row">
    <span>旋转 <span class="tooltip-icon" data-tooltip="旋转角度 -180~180">?</span></span>
    <span id="rotationValueDisplay">0</span>
    <input type="range" id="rotationSlider" min="-180" max="180" step="1">
</div>
<div class="slider-row">
    <span>透明度</span>
    <span id="opacityValueDisplay">100%</span>
    <input type="range" id="opacitySlider" min="0" max="100" step="1">
</div>
```

- [ ] **Step 2: `updatePropsPanelWithElement` 接线**

在现有字号/重复滑块接线后追加：
```js
const rot = parseFloat(el.getAttribute('data-rotation')) || 0;
const rotSlider = document.getElementById('rotationSlider');
const rotVal = document.getElementById('rotationValueDisplay');
rotSlider.value = rot; rotVal.textContent = rot;
rotSlider.oninput = (e) => {
    const v = parseInt(e.target.value);
    rotVal.textContent = v;
    el.setAttribute('data-rotation', v);
    el.style.transform = v ? `rotate(${v}deg)` : '';
    pushHistory();
};
const op = parseFloat(el.getAttribute('data-opacity')) || 1;
const opSlider = document.getElementById('opacitySlider');
const opVal = document.getElementById('opacityValueDisplay');
opSlider.value = Math.round(op * 100); opVal.textContent = Math.round(op * 100) + '%';
opSlider.oninput = (e) => {
    const v = parseInt(e.target.value) / 100;
    opVal.textContent = e.target.value + '%';
    el.setAttribute('data-opacity', v);
    el.style.opacity = v;
    pushHistory();
};
```

- [ ] **Step 3: 验证**

- 选中元素调旋转 → 元素旋转，滑块/显示同步；调透明度 → 生效。
- 旋转后四角缩放手柄仍可用（贴包围盒）；旋转后拖动元素正常。
- 保存/加载后 rotation/opacity 保留。
- 非 emoji 染色不受影响。

- [ ] **Step 4: Commit**

```bash
git add "字符作画/字符作画.html" "字符作画/字符作画.js"
git commit -m "增强：字符作画页元素旋转与透明度调节"
```

---
---

### Task 4: 方向键微调

**Files:**
- Modify: `字符作画/字符作画.js`

**Interfaces:**
- Consumes: `handleBackspaceDelete`（全局 keydown）、`currentSelected`
- Produces: `moveSelectedBy(dx, dy)` 辅助函数（Task 5 扩展为多选共用）

**Goal:** 选中后方向键移 1px、Shift+方向键 10px；松开时合并为一个撤销快照。

- [ ] **Step 1: 加 `moveSelectedBy` 辅助**

```js
function moveSelectedBy(dx, dy) {
    const targets = getSelectionTargets();   // Task 4 先返回 [currentSelected]，Task 5 扩展为多选集合
    targets.forEach(t => {
        t.style.left = (parseInt(t.style.left) || 0) + dx + 'px';
        t.style.top = (parseInt(t.style.top) || 0) + dy + 'px';
    });
    if (currentSelected) {
        document.querySelectorAll('.resize-handle').forEach(h => updateHandlePosition(h, currentSelected));
    }
}
function getSelectionTargets() { return currentSelected ? [currentSelected] : []; }
```

- [ ] **Step 2: `handleBackspaceDelete` 加方向键分支**

在函数开头（Ctrl+Z/Y 分支之后）：
```js
const ARROWS = { ArrowUp: [0,-1], ArrowDown: [0,1], ArrowLeft: [-1,0], ArrowRight: [1,0] };
if (ARROWS[e.key] && currentSelected) {
    e.preventDefault();
    const step = e.shiftKey ? 10 : 1;
    moveSelectedBy(ARROWS[e.key][0] * step, ARROWS[e.key][1] * step);
    arrowHistoryDirty = true;
    return;
}
```
声明 `let arrowHistoryDirty = false;`。在 `init()` 加 keyup 合并：
```js
window.addEventListener('keyup', (e) => {
    if (ARROWS[e.key] && arrowHistoryDirty) { pushHistory(); arrowHistoryDirty = false; }
});
```
（`ARROWS` 需在 init 可访问——提到模块顶层声明。）

- [ ] **Step 3: 验证**

- 选中元素方向键每次移 1px、Shift+10px；按住连续移动，松开只记一次撤销。
- 方向键不再滚动页面（preventDefault）。
- Backspace/Delete/Ctrl+Z 逻辑不受影响。

- [ ] **Step 4: Commit**

```bash
git add "字符作画/字符作画.js"
git commit -m "增强：字符作画页方向键微调元素位置"
```

---
---

### Task 5: 多选（选中模型重构 + 移动 + 删除）

**Files:**
- Modify: `字符作画/字符作画.html`（props 面板加多选态视图）
- Modify: `字符作画/字符作画.js`（selectedSet 重构）

**Interfaces:**
- Consumes: 现有 `selectElement`、`makeDraggable`、`handleBackspaceDelete`、`updatePropsPanelWithElement`
- Produces: `selectedSet`、`getSelectionTargets()`（Task 4 的函数升级）、`#multiSelectPanel`（"已选 N 个"视图）

**Goal:** Ctrl+点击多选；拖动任一选中整体移动；批量删除；多选时属性面板显示"已选 N 个"。

- [ ] **Step 1: 选中模型重构**

模块顶层加 `const selectedSet = new Set();`（保留 `currentSelected` 作主锚点）。

改造 `selectElement(el, opts)`：
```js
function selectElement(el, opts = {}) {
    if (currentSelected) { currentSelected.classList.remove('selected'); hideResizeHandles(); }
    if (!el) { selectedSet.clear(); currentSelected = null; }
    else if (opts.additive) {
        // Ctrl+点击：切换
        if (selectedSet.has(el)) selectedSet.delete(el);
        else { selectedSet.add(el); el.classList.add('selected'); }
        currentSelected = selectedSet.size ? [...selectedSet][selectedSet.size - 1] : null;
    } else {
        selectedSet.clear(); selectedSet.add(el); currentSelected = el; el.classList.add('selected');
    }
    document.querySelectorAll('.scene-item').forEach(i => {
        if (!selectedSet.has(i)) i.classList.remove('selected');
    });
    // 面板
    const propsDiv = document.getElementById('propsPanel');
    const multiDiv = document.getElementById('multiSelectPanel');
    const selDiv = document.getElementById('selectionControls');
    if (currentSelected) {
        propsDiv.style.display = 'block';
        if (selectedSet.size > 1) {
            if (multiDiv) multiDiv.style.display = 'block';
            selDiv.style.display = 'none';
            if (multiDiv) multiDiv.querySelector('#multiCount').textContent = selectedSet.size;
        } else {
            if (multiDiv) multiDiv.style.display = 'none';
            selDiv.style.display = 'block';
            showResizeHandles(currentSelected);
            updatePropsPanelWithElement(currentSelected);
        }
    } else {
        propsDiv.style.display = 'none';
    }
    if (typeof refreshLayers === 'function') refreshLayers();
}
```
`getSelectionTargets()` 升级为 `[...selectedSet]`（size>0 时），否则 `[currentSelected]`（过滤 null）。

- [ ] **Step 2: 元素点击传 ctrl 态**

`createCharacterDiv` 内的 click 监听：
```js
div.addEventListener('click', (e) => { e.stopPropagation(); selectElement(div, { additive: e.ctrlKey || e.metaKey }); });
```

- [ ] **Step 3: 多选拖动**

改造 `makeDraggable`：mousedown 时若 `selectedSet.has(element) && selectedSet.size > 1`，记录全部选中元素的起始 left/top；mousemove 用 delta 同时更新全部；mouseup 一次 `pushHistory()`。

- [ ] **Step 4: 批量删除**

`handleBackspaceDelete` 的 Delete/Backspace 分支：删除 `getSelectionTargets()` 全部元素（重复>1 时只减主锚点重复）；`selectElement(null)`；`pushHistory()`。

- [ ] **Step 5: props 面板加多选视图**

`字符作画.html` 的 `#selectionControls` 之前加（默认隐藏）：
```html
<div id="multiSelectPanel" style="display:none;">
    <div class="preview-card">
        <div class="preview-char" style="font-size:20px;" id="multiCount">已选 2 个</div>
        <button class="btn danger" id="multiDeleteBtn">🗑️ 批量删除</button>
        <button class="btn" id="multiCancelBtn">取消选择</button>
    </div>
</div>
```
JS：`multiDeleteBtn` → 删除全部选中 + pushHistory + selectElement(null)；`multiCancelBtn` → selectElement(null)。

- [ ] **Step 6: 验证**

- Ctrl+点击多选/取消；单选恢复；多选时面板显示"已选 N 个"。
- 拖动任一选中整体移动，单次撤销。
- 批量删除、Backspace/Delete、方向键微调（Task 4 联动）都作用于全部选中。
- 画布空白点击取消全部选中。

- [ ] **Step 7: Commit**

```bash
git add "字符作画/字符作画.html" "字符作画/字符作画.js"
git commit -m "增强：字符作画页多选（移动/删除）+ 选中模型重构"
```

---
---

### Task 6: 图层面板（叠在素材库下方，可折叠）

**Files:**
- Modify: `字符作画/字符作画.html`（悬浮面板底部加图层区）
- Modify: `字符作画/字符作画.js`（refreshLayers + 行交互）
- Modify: `字符作画/字符作画.css`（图层区样式）

**Interfaces:**
- Consumes: `selectedSet`/`currentSelected`（Task 5）、z-index 排序
- Produces: `refreshLayers()`；元素 ID `layerPanel/layerToggleBtn/layerList`

**Goal:** 悬浮面板底部图层区（默认收起），按 z-index 倒序展示全部元素，支持点击选中双向同步、↑↓ 调层级、删除。属性面板原"往前/退后"按钮移除。

- [ ] **Step 1: HTML 加图层区**

`字符作画.html` 的 `#libraryPanel` 之后（同浮层面板内）加：
```html
<div id="layerPanel">
    <div class="panel-header">
        <span>🎨 图层</span>
        <button class="layer-toggle" id="layerToggleBtn" title="展开/收起">▶</button>
    </div>
    <div id="layerList" class="layer-list" style="display:none;"></div>
</div>
```

- [ ] **Step 2: JS `refreshLayers()`**

```js
function refreshLayers() {
    const list = document.getElementById('layerList');
    if (!list) return;
    list.innerHTML = '';
    const items = [...document.querySelectorAll('#sceneCanvas .scene-item')]
        .sort((a, b) => (parseInt(b.style.zIndex) || 0) - (parseInt(a.style.zIndex) || 0));
    items.forEach((el, i) => {
        const row = document.createElement('div');
        row.className = 'layer-item' + (selectedSet.has(el) ? ' active' : '');
        const ch = el.getAttribute('data-char') || '?';
        row.innerHTML = `<span class="layer-char">${ch.slice(0,1)}</span>
            <span class="layer-name">元素 ${items.length - i}</span>
            <button class="layer-up" title="上移一层">↑</button>
            <button class="layer-down" title="下移一层">↓</button>
            <button class="layer-del" title="删除">🗑</button>`;
        row.addEventListener('click', (e) => {
            if (e.target.closest('button')) return;
            selectElement(el, { additive: e.ctrlKey || e.metaKey });
        });
        row.querySelector('.layer-up').addEventListener('click', (e) => { e.stopPropagation(); swapLayerZ(el, -1); });
        row.querySelector('.layer-down').addEventListener('click', (e) => { e.stopPropagation(); swapLayerZ(el, 1); });
        row.querySelector('.layer-del').addEventListener('click', (e) => { e.stopPropagation(); el.remove(); selectElement(null); pushHistory(); refreshLayers(); });
        list.appendChild(row);
    });
}
function swapLayerZ(el, dir) {
    const items = [...document.querySelectorAll('#sceneCanvas .scene-item')]
        .sort((a, b) => (parseInt(a.style.zIndex) || 0) - (parseInt(b.style.zIndex) || 0));
    const idx = items.indexOf(el);
    const other = items[idx + dir];
    if (!other) return;
    const t = el.style.zIndex; el.style.zIndex = other.style.zIndex; other.style.zIndex = t;
    el.setAttribute('data-zindex', el.style.zIndex);
    other.setAttribute('data-zindex', other.style.zIndex);
    pushHistory(); refreshLayers();
}
```
图层区折叠交互：`layerToggleBtn` 切换 `layerList` 显隐 + `▶/▼`。

- [ ] **Step 3: `refreshLayers()` 触发点**

在 `addSymbolToCanvas`、`applySnapshot`、`deleteItemBtn`/`multiDeleteBtn` 删除后、`clearCanvas`、`newScene`、以及 `swapLayerZ` 后调用 `refreshLayers()`（`selectElement` 已含，Task 5 Step 1）。`init()` 末尾首次调用。

- [ ] **Step 4: 移除属性面板"往前/退后"**

`字符作画.html` 删 `#raiseBtn`/`#lowerBtn` 两个按钮；`字符作画.js` 删 `updatePropsPanelWithElement` 中对应的 onclick 绑定（`raiseBtn`/`lowerBtn`）。

- [ ] **Step 5: CSS 图层区**

```css
.layer-toggle { background:none; border:none; cursor:pointer; color: var(--text-muted); font-size:0.8rem; padding:0 4px; }
.layer-list { margin-top:0.5rem; max-height: 200px; overflow-y:auto; }
.layer-item { display:flex; align-items:center; gap:6px; padding:4px 6px; border-radius: var(--radius-md); cursor:pointer; }
.layer-item:hover { background: var(--bg-subtle); }
.layer-item.active { background: var(--accent-light); color: var(--accent-primary); }
.layer-char { font-size:1.1rem; width:1.5em; text-align:center; font-family:'Segoe UI Emoji','Apple Color Emoji',monospace; }
.layer-name { flex:1; font-size:0.8rem; color: var(--text-secondary); }
.layer-item button { background:none; border:none; cursor:pointer; padding:0 2px; color: var(--text-muted); font-size:0.75rem; }
.layer-item button:hover { color: var(--accent-primary); }
```

- [ ] **Step 6: 验证**

- 图层区默认收起，展开后按 z-index 倒序列出全部元素；点击行选中（画布同步高亮）；选中画布元素 → 对应行高亮。
- ↑↓ 交换层级；删除行删除元素。
- 添加/删除/撤销重做后图层列表刷新。
- 属性面板"往前/退后"已移除，无残留绑定报错。

- [ ] **Step 7: Commit**

```bash
git add "字符作画/字符作画.html" "字符作画/字符作画.js" "字符作画/字符作画.css"
git commit -m "增强：字符作画页图层面板（叠素材库下方，可折叠）"
```

---
---

### Task 7: 三层验证流水线

**Files:** 无（验证）

**Goal:** 静态检查 + 浏览器功能回归 + 视觉确认，全部通过后才可向用户汇报。

- [ ] **Step 1: 静态检查（dispatch verify）**

prompt 说明：改动了 `字符作画/字符作画.{html,css,js}`（阶段 2 增强：缩放/旋转/透明度/图层面板/方向键微调/多选）。检查：语法、花括号平衡、新增 ID（zoomOutBtn/zoomDisplay/zoomInBtn/zoomResetBtn/rotationSlider/rotationValueDisplay/opacitySlider/opacityValueDisplay/multiSelectPanel/multiCount/multiDeleteBtn/multiCancelBtn/layerPanel/layerToggleBtn/layerList）在 HTML 与 JS 中一一对应、CSS 变量均在 common.css 定义、无残留内联 style/script。

- [ ] **Step 2: 浏览器功能回归（dispatch testing）**

改动文件: `字符作画/字符作画.html`、`字符作画/字符作画.css`、`字符作画/字符作画.js`
改动意图: 阶段 2 新增画布缩放/旋转/透明度/图层面板/方向键微调/多选；阶段 1 的骨架/悬浮面板/编辑功能不应受影响
关键区域: 工具栏缩放组、属性面板（字号/重复/颜色/旋转/透明度）、悬浮面板（素材库+图层区）、画布拖放/移动/缩放/撤销重做
不应受影响: 阶段 1 全部既有编辑功能（拖放/移动/缩放/重复/变色/撤销重做/存读/渐变背景/帮助弹窗）；悬浮面板拖动/折叠；旧 localStorage 场景兼容

testing 负责：Playwright 打开页面，检查控制台、DOM、逐项交互（含 Ctrl+滚轮缩放、Ctrl+点击多选、方向键微调、图层↑↓、旋转/透明度滑块、保存/加载往返、旧数据兼容），截图存 `根目录/tmp/`。

- [ ] **Step 3: 视觉确认 + 汇报**

截图确认：工具栏缩放组、属性面板新滑块、图层区收起/展开态。向用户汇报结果，等待"验收通过"后再收尾（spec/plan 提交、决策日志更新）。

---
---

## 自审清单（写计划时已核对）

- Spec 覆盖：5.1~5.6 六项 + 六（数据模型）+ 七（缩放连锁）全部有任务对应。
- 类型/签名一致：`getSelectionTargets()` 在 Task 4 定义、Task 5 扩展；`refreshLayers()` 在 Task 5 的 `selectElement` 中调用、Task 6 定义（typeof 守卫）；`data-rotation/data-opacity` 在 Task 1 定义、Task 3 消费。
- 无占位符：每任务含具体改动与验证。
- 阶段 1 决策沿用：坐标 API 为画布相对坐标（drop 换算 ÷ zoom）、悬浮面板布局不变、原生 DOM 渲染。
