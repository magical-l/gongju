功能
	✓ 1，大于500个字符的标签，可以靠翻页遍历所有字符，而不是只能看前500个。
	✓ 2，左侧标签树，每个节点都显示字符数量，没有就显示0，非叶子节点的字符数包含下属所有叶子节点的字符数。数字不要用逗号分隔。
	✓ 3，通过unicode码、html转义字符串搜字符。
	✓ 4，多标签搜索。（v1.9.1：空格分词、标签∪字符名匹配、交集节 + 逐 token 节独立分页）
优化
	✓ 1，左侧标签树重排：把“区块”放最后，“官方分类”倒数第二，“文字系统”倒数第三。
	✓ 2，左侧标签树，标签节点不显示简介。在中部符号区的上方，当前选中标签名字下显示简介。
	✓ 3，左侧标签树改用details+summary这些原生标签。
	✓ 4，左侧标签树宽度太小，最好能显示得下最长的标签而不用出现x滚动条。
	5，右侧字符详情界面，所属标签不用显示全路径。
	✗ 6，考虑在中间列表区也可以切换字体。：目的不明确。
	✓ 7，列表区符号卡片显示名字。（v1.0.0 迁移时已拆）
	✓ 8，分页“total”改为“共”（已建 emoji 根 + emoji-text双模 子标签）
	✓ 9，标签也需要加“别名”了，否则搜索有点难。或者说，左侧标签树对于标签名更宽容一些，可以容纳更长的标签名，换行显示？
	✓ 10，搜索也带上简介。
	✓ 11，显示不了的字符能不能不显示为豆腐块？
	✓ 12，相同的标签，简介改为相同。比如“文字系统/西夏文”和“区块/西夏文”。
	✓ 13，中间列表区的字符卡片，高度最好取一个全局最高的高度固定一下。
	✓ 14，字体栈统一为系统优先，Segoe UI Emoji/Symbol 优先、Noto Sans Symbols 2 兜底，精简为5个；右侧字体切换支持"加载本机字体"。
	✓ 15，右侧栏需要加个按钮，清除选中的字体，回归默认的字体栈。
	16，搜索后，点击分类则取消搜索，清除搜索结果
	✓ 17，左侧树节点，改为：点击三角箭头才收展下级节点，但不导航到该标签；点击三角箭头右边区域导航到该标签，但不收展。
	✓ 18，左侧树节点，没有子节点的节点要在名字左侧预留三角标记的位置。
	✓ 20，右侧栏顶部显示符号时，对于双模字符，没有显示出来。（v1.8.5：顶部对双模显示 text+emoji 双变体 + 跳过豆腐/Noto 检测）
	✓ 21，如果选择非叶子节点，则在中间列表区也分段展示，先展示不属于任何下级节点的字符，然后依次展示每个下级节点的字符，每个下级节点占节。（v1.7.0：每段预览前 30 个 + "查看更多 →"跳转该标签完整网格页；只分一层）
	✓ 22，列表区的“本级”要显示全，不用“查看更多”。（v1.8.2：本级段全量渲染 + 无查看更多按钮）
	✓ 23，“查看更多”移到名字后面（v1.8.2：移到段头名字后，保持按钮样式）
bug
	✓ 1，搜”月亮 emoji”，在”多标签交集”里，还会出现☊☋等，但它们并非emoji啊。（v1.12.1：简介命中不进字符结果，仅留标签导航）

数据清理

标签系统 v1.0.0 收尾
	✓ 标签树 v1.0.0 定稿（标签.txt 为权威设计文档）
	✓ 标签.json 按新树重建 + 旧数据迁移（build_tags.py 可重跑）
	✓ emoji 批量打标：单码位细化到叶子 + 双模 + emoji 根（tag_emoji.py 可重跑）
	✓ 页面临时编辑功能：列表区字符 ✎ 移动/添加 + 操作记录面板 + 复制导出（纯内存）
	✓ apply_ops.py：把复制的操作记录应用回 标签.json
	✓ 待办：数据清理 items 2-6、10 已修正（7、8、9 早已完成）；item 11 性别部分完成。
	✓ 待办：ZWJ 序列（家庭/职业/发型等 471 个）暂未打标。（v1.12.0 完成）
		· 问题本质：3+码位 ZWJ 序列（如 🧑🦰 = 1F9D1+200D+1F9B0）无法打标。
		· 原因：页面网格只支持单码位 ranges + 双码位旗帜 seqs；硬塞会把 U+200D/U+FE0F 当字符加进 ranges（污染，踩过坑）。
		· 需要：网格支持 seqs 显示（功能开发，涉及网格渲染/分页/详情）后才能打标。
		· 已补：单码位发色组件 🦰🦱🦲🦳 已挂"发型、发色"。
		· 实现（v1.12.0）：seqs 泛化（seqCps/seqMeta 整串键，任意长度码位）+ build_zwj.py 从 emoji-test.txt 注入 1614 全量序列（CLDR zh + 程序化兜底中文名）+ 肤色维度（人/肤色/浅~深肤色 5 子节点，双肤色多挂 + 修饰符单码位）+ emoji 挂载 + 连字检测（不连字/单字符豆腐块标记"当前字体不支持"，严格单字符检测 _strictLocalCan 含 emoji 区）+ 双肤色命名（x肤色人牵手y肤色人 / x肤色人与y肤色人戴兔耳）+ 分页 100 + 概览本级分页 + 顶部/效果预览/字体切换处理。
	待办：数据文件加 ?v= 版本参数（标签.json / 中文名.json / 名字.json / noto-cmap.json / 符号数据.js）。
		· 原因：不能依赖服务器缓存头（本地服务器可能换/别人用别的服务器）；现改动频繁先缓缓，稳定后统一加。
		· 方式：与 符号.js 惯例一致 ?v=YYMMDD；fetch 和 script src 都要加。
