功能
	5，……
优化
	5，右侧字符详情界面，所属标签不用显示全路径。
	✗ 6，考虑在中间列表区也可以切换字体。：目的不明确。
	16，搜索后，点击分类则取消搜索，清除搜索结果
	29，……

bug
	暂无

数据清理
	✓ 🚣‍♂️官方名是man rowing boat，但汉语主名却是“划船”，丢失了“man”这个信息。应该在“水上运动”里叫“男人划船”，别名是（男人、男子、男生）*（划船、划艇），在“运动的人”里叫“划船男人”，别名同理。类似的符号很多。
		（**v1.38.0**：根因是 build_zwj.py 对未命中兜底的序列直接取 CLDR 短名，三条序列撞名。序列名改走**机械直译**、CLDR 退出名字层；补「人称同义词×动作同义词」别名（134 组）+ 人工筛过的 CLDR 俗名 30 条；体育类序列**双挂**「体育、运动/<项>」（主题轴）与「人/人物角色/运动的人」（角色轴），两条轴下各叫各名。落地记录见 docs/2026-09-15-符号页-序列命名分层与双轴语境名.md）

标签系统 v1.0.0 收尾
	✓ 标签树 v1.0.0 定稿（标签.txt 为权威设计文档）
	✓ 标签.json 按新树重建 + 旧数据迁移（build_tags.py 可重跑）
	✓ emoji 批量打标：单码位细化到叶子 + 双模 + emoji 根（tag_emoji.py 可重跑）
	✓ 页面临时编辑功能：列表区字符 ✎ 移动/添加 + 操作记录面板 + 复制导出（纯内存）
	✓ apply_ops.py：把复制的操作记录应用回 标签.json
	✓ 待办：数据清理 items 2-6、10 已修正（7、8、9 早已完成）；item 11 性别部分完成。
	✓ 待办：ZWJ 序列（家庭/职业/发型等 471 个）暂未打标。（v1.12.0 完成）
		· 问题本质：3+码位 ZWJ 序列（如 🧑🦰 = 1F9D1+200D+1F9B0）无法打标。
		· 原因：页面网格只支持单码位 ranges + 双码位旗帜 seqs；硬塞会把 U+200D/U+FE0F 当字符加进 ranges（污染，踩过坑）。
		· 需要：网格支持 seqs 显示（功能开发，涉及网格渲染/分页/详情）后才能打标。
		· 已补：单码位发色组件 🦰🦱🦲🦳 已挂"发型、发色"。
		· 实现（v1.12.0）：seqs 泛化（seqCps/seqMeta 整串键，任意长度码位）+ build_zwj.py 从 emoji-test.txt 注入 1614 全量序列（CLDR zh + 程序化兜底中文名）+ 肤色维度（人/肤色/浅~深肤色 5 子节点，双肤色多挂 + 修饰符单码位）+ emoji 挂载 + 连字检测（不连字/单字符豆腐块标记"当前字体不支持"，严格单字符检测 _strictLocalCan 含 emoji 区）+ 双肤色命名（x肤色人牵手y肤色人 / x肤色人与y肤色人戴兔耳）+ 分页 100 + 概览本级分页 + 顶部/效果预览/字体切换处理。
		变体折叠卡片（v1.13.0，2026-08-28 完成）
			✓ 网格/概览/搜索把"同款不同肤色"的 ZWJ 序列折叠成一张卡（主符号 + 肤色变体下拉），消除笛卡尔积平铺冗余（人/职业 348→60 卡）。
			· 分组键 = 去肤色码位；性别不折叠（男/女/中性各自成卡）；单成员组无按钮；旗帜/单码位不折叠。
			· 卡片 main = 无肤色成员优先（无则取第一个）；卡片下方"肤色变体▾ N"按钮 → dropdown 列全部变体（略小卡），点选进详情、点外部关闭。
			· 分页/计数按卡；搜索 main 优先取命中变体；数据文件零改动。
				（v1.37.0 修正：原来是 `members.find(命中preferKeys的)`，取的是列表里第一个命中项，而 members 顺序来自各标签 seqs 的并集顺序——搜「划船」时浅肤色那条排在前面就被选中了，**尽管无肤色那条也命中**。改成命中项先聚成数组、再从里面优先挑无肤色的。卡片 main 的无肤色优先现在在「多个命中」时也生效。）
	待办：数据文件加 ?v= 版本参数（标签.js / 官方名直译名.js / unicode官方名.js / noto-cmap.js / 符号富化数据.js）。
		· 原因：不能依赖服务器缓存头（本地服务器可能换/别人用别的服务器）；现改动频繁先缓缓，稳定后统一加。
		· 方式：与 符号.js 惯例一致 ?v=YYMMDD；fetch 和 script src 都要加。

	折叠卡整组编辑（v1.14.0，2026-08-28 完成）
		✓ 折叠卡（有肤色变体）的 ✎ 打标/拖拽移动/取消打标时，主符号+所有肤色变体整组一体操作（变体跟随）。
		· 协议：整组 payload.cps 传对象数组成员列表 [{cps,zh,en},...]（每成员带名字）；op 记 1 条，opChar/cpsHex 显示主符号。
		· 修复：服务器 _add_member 识别数组的数组逐个处理（防坏数据）+ seqs_add 防御性排序（遇 dict 不崩）；前端 cellText/variantGroupKey/groupSeqsByBase 嵌套数据防御（防 NaN）。
		· 修复：men/women/people wrestling 与 with bunny ears 命名区分（摔跤男人/女人/人，原全叫"摔跤的人"）。

