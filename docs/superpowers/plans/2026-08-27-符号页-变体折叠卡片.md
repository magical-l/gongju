# 符号页 变体折叠卡片 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 本项目规则（用户 CLAUDE.md）要求 HTML/CSS/JS 修改一律 dispatch `html-editor` 子代理；实现后按项目管线 4.1（html-editor 自闭环语法检查）→ 4.2（verify 静态检查）→ 4.3（testing Playwright 功能验证）执行。Steps 使用 `- [ ]` 语法跟踪。

**Goal:** 网格/概览/搜索结果把"同款不同肤色"的 ZWJ 序列折叠成一张卡（主符号 + 肤色点行），消除笛卡尔积平铺冗余。

**Architecture:** 纯展示层折叠——`标签.json` 数据一条不动。在 符号.js 加模块级 helper（肤色码位集 / 分组键 / 组→卡 / 卡窗口 / 卡→成员平铺），`gridItems` 拆成 `gridCards`（卡片数组，供模板）+ `gridItems`（卡内成员平铺，供渲染检测/选中检测/拖拽）；`overviewSegments`、`searchSections` 同步改卡片。主符号 = 组内无肤色成员优先（无则第一个），点行 = 其余变体（最多 8 个 + 溢出 `+N` 面板）。

**Tech Stack:** Vue 3 global build、Element Plus、vanilla JS；无构建、无 npm（禁止创建 package.json / node_modules）。

## Global Constraints

- 数据零改动：`标签.json` / `中文名.json` / `名字.json` 一条不动。
- 分组键 = 序列去掉肤色码位（U+1F3FB–1F3FF）后的整串；**性别不折叠**（男/女/中性首位人码位不同 → 各自成卡）。
- 卡片 `{ main, dots, overflow }`：`main` 无肤色优先（无则取组内第一个）；`dots` 全部变体（按组内原序，模板最多显示 8 个）；`overflow = max(0, dots.length - 8)`。
- 生效三处：标签网格（`gridCards`）、概览分段（`overviewSegments`）、搜索结果（`searchSections`，main 优先取搜索命中变体）。
- 分页/计数**按卡**计（每页 100 卡）。
- 单成员组无点行（视觉与现在一致）；旗帜/单码位（无肤色变体）不折叠。
- 点行忠实渲染真实字符（豆腐/不连字照实显示）；点行不可拖拽、无 ✎。
- 纯前端三文件：`符号.js` / `符号.html` / `符号.css`。缓存版本号递增（`符号.js?v=260833→260840`、`符号.css?v=260836→260840`）。
- 收尾（版本号 + 文档 + git commit）只在用户明确验收后执行（项目约定，不在任务内 commit）。
- 新增数据 state/方法命名须与既有风格一致（tab 缩进、双引号、`/** */` 注释）。

---

### Task 1: 核心抽象 + 标签网格折叠

**Files:**
- Modify: `符号/符号.js` — 模块级 helper（seqMeta 后）+ `selectedCount` / `gridItems`→`gridCards` + `gridPageCount`
- Modify: `符号/符号.html` — 主网格模板（`gridItems` → `gridCards` 卡片渲染）
- Modify: `符号/符号.css` — 点行 / 点 / 溢出按钮样式

**Interfaces:**
- Consumes: 既有 `seqCps`、`seqMeta`、`rangeCount`、`enumerateWindow`、`gridPage`/`gridPageSize`/`selectedMembers`/`selectedChar`/`cellKey`/`cellText`/`renderState`/`itemDual`/`isSelected`/`itemTitle`/`gridItemName`/`isControlCode`/`selectItem`/`onDragStart`/`openTagEditor`/`copyGridVariant`
- Produces:
  - `SKIN_CPS`（Set，模块级）：5 个肤色码位
  - `variantGroupKey(cps) → string`：去肤色后 `join('-')`
  - `groupSeqsByBase(seqs) → Map<string, number[][]>`：按组键分组（组内保持 seqs 原序）
  - `makeCard(members) → {main, dots, overflow}`：main 无肤色优先
  - `cardWindow(ranges, seqs, start, size) → card[]`：单码位逐码位成卡 + seqs 变体组折叠成卡
  - `cardsToMembers(cards) → (number|number[])[]`：卡片平铺成员
  - `gridCards()` computed（卡片数组，模板用）；`gridItems()` computed = `cardsToMembers(gridCards)`（内部平铺，保持既有消费方语义）；`selectedCount()` = 卡片总数

- [ ] **Step 1: 符号.js 加模块级 helper**

在 `seqMeta`（约 94 行）后插入：

```js
// ---- 变体折叠（v1.13.0）----
/** 肤色修饰符码位（U+1F3FB–1F3FF）：变体折叠的分组依据 */
const SKIN_CPS = new Set([0x1F3FB, 0x1F3FC, 0x1F3FD, 0x1F3FE, 0x1F3FF]);

/** 序列变体分组键：去掉肤色码位后的整串（同款不同肤色同组） */
function variantGroupKey(cps) {
	return cps.filter(c => !SKIN_CPS.has(c)).join('-');
}

/** seqs → 变体组：Map<组键, 纯码位数组[]>（组内保持 seqs 原序） */
function groupSeqsByBase(seqs) {
	const groups = new Map();
	for (const s of seqs) {
		const cps = seqCps(s);
		const key = variantGroupKey(cps);
		if (!groups.has(key)) groups.set(key, []);
		groups.get(key).push(cps);
	}
	return groups;
}

/** 组内成员 → 卡片 { main, dots, overflow }：main=无肤色优先（无则第一个），dots=其余全部（原序），overflow=超 8 计数 */
function makeCard(members) {
	if (members.length === 1) return { main: members[0], dots: [], overflow: 0 };
	let main = members.find(cps => !cps.some(c => SKIN_CPS.has(c)));
	if (main === undefined) main = members[0];
	const dots = members.filter(cps => cps !== main);
	return { main, dots, overflow: Math.max(0, dots.length - 8) };
}

/** 网格卡片窗口：单码位逐码位成卡（前），seqs 变体组折叠成卡（后）；取 [start, start+size) 张卡 */
function cardWindow(ranges, seqs, start, size) {
	const singleTotal = rangeCount(ranges);
	const cards = [];
	if (start < singleTotal) {
		for (const cp of enumerateWindow(ranges, start, size)) cards.push({ main: cp, dots: [], overflow: 0 });
	}
	if (cards.length < size) {
		const groups = [...groupSeqsByBase(seqs).values()];
		const seqStart = Math.max(0, start - singleTotal);
		const seqEnd = Math.min(groups.length, seqStart + (size - cards.length));
		for (let i = seqStart; i < seqEnd; i++) cards.push(makeCard(groups[i]));
	}
	return cards;
}

/** 卡片列表 → 平铺成员（主符号 + 全部变体；供渲染能力检测 / 选中检测 / 拖拽） */
function cardsToMembers(cards) {
	const out = [];
	for (const c of cards) {
		out.push(c.main);
		for (const d of c.dots) out.push(d);
	}
	return out;
}
```

- [ ] **Step 2: 符号.js 改 `selectedCount` / `gridItems` → `gridCards`**

`selectedCount`（744 行）改为卡片总数：

```js
		/** 当前选中标签聚合后的卡片总数（单码位逐码位 + seqs 变体组折叠） */
		selectedCount() {
			const m = this.selectedMembers;
			return rangeCount(m.ranges) + groupSeqsByBase(m.seqs).size;
		},
```

`gridItems`（748 行）整体替换为两个 computed：

```js
		/** 当前选中标签的网格卡片：按页窗口切片（单码位在前，变体组衔接其后，每页最多 CAP 张卡） */
		gridCards() {
			const m = this.selectedMembers;
			if (!m.ranges.length && !m.seqs.length) return [];
			const start = (this.gridPage - 1) * this.gridPageSize;
			return cardWindow(m.ranges, m.seqs, start, this.gridPageSize);
		},
		/** 当前页卡片的平铺成员（主符号+全部变体；渲染检测/选中检测用，保持 gridItems 语义） */
		gridItems() {
			return cardsToMembers(this.gridCards);
		},
```

`gridPageCount`（796 行）不变（`selectedCount` 已是卡片数）。

- [ ] **Step 3: 符号.html 主网格模板改卡片渲染**

将 160–177 行主网格块替换为：

```html
							<div class="char-grid" v-else-if="selectedTag && gridCards.length">
								<div class="char" v-for="card in gridCards" :key="cellKey(card.main)" :draggable="editEnabled" :class="{ sel: isSelected(card.main), 'has-variants': itemDual(card.main), 'is-ctrl': isControlCode(card.main) }" :title="itemTitle(card.main)" @click="selectItem(card.main)" @dragstart="onDragStart(card.main, $event)" @dragend="onDragEnd">
									<button v-if="editEnabled" class="tag-edit-btn" @click.stop="openTagEditor(card.main)" title="移动/添加到其他标签">✎</button>
									<span class="char-symbol" :class="{ tofu: renderState(card.main) === false }">
										<template v-if="renderState(card.main) === true">
											<template v-if="itemDual(card.main)">
												<span class="variant text vs-text" @click.stop="copyGridVariant(card.main, 'text')">{{ variantText(cellText(card.main), 'text') }}</span>
												<span class="variant emoji" @click.stop="copyGridVariant(card.main, 'emoji')">{{ variantText(cellText(card.main), 'emoji') }}</span>
											</template>
											<template v-else>{{ cellText(card.main) }}</template>
										</template>
										<span v-else-if="renderState(card.main) === false" class="tofu-hint">当前字体不支持</span>
										<span v-else class="tofu-pending" aria-hidden="true"></span>
									</span>
									<span class="char-name">{{ gridItemName(card.main) }}</span>
									<div v-if="card.dots.length" class="char-dots" @dragstart.stop.prevent @click.stop>
										<span v-for="d in card.dots.slice(0, 8)" :key="cellKey(d)" class="char-dot" :class="{ sel: isSelected(d) }" :title="itemTitle(d)" @click.stop="selectItem(d)">{{ cellText(d) }}</span>
										<span v-if="card.overflow" class="char-dot-more" title="查看全部变体" @click.stop="openVariantPanel(card)">+{{ card.overflow }}</span>
									</div>
									<span v-if="isControlCode(card.main)" class="ctrl-badge">控制码</span>
								</div>
							</div>
```

182 行空态判断 `gridItems.length === 0` 改为 `gridCards.length === 0`。

- [ ] **Step 4: 符号.css 加点行样式**

在 `.char-grid .char .ctrl-badge`（约 440 行）后追加（**平铺选择器**，与 `.char-grid .char .variant` 等规则同级；`符号.css` 该区为扁平非 `&` 嵌套）：

```css
.char-grid .char .char-dots {
	display: flex;
	align-items: center;
	justify-content: center;
	gap: 2px;
	margin-top: 1px;
	min-height: 16px;
}

.char-grid .char .char-dot {
	display: inline-flex;
	align-items: center;
	justify-content: center;
	min-width: 16px;
	height: 16px;
	padding: 0 1px;
	font-size: 12px;
	line-height: 1;
	border-radius: 50%;
	border: 1px solid transparent;
	background: #f2f7fa;
	cursor: pointer;
}

.char-grid .char .char-dot:hover {
	border-color: #4a90d9;
	background: #e8f1fb;
}

.char-grid .char .char-dot.sel {
	border-color: #4a90d9;
	background: #dbe9f8;
}

.char-grid .char .char-dot-more {
	font-size: 9px;
	line-height: 1;
	padding: 3px 4px;
	border-radius: 3px;
	border: 1px solid #c8d4e0;
	color: #5f7a8d;
	background: #f2f7fa;
	cursor: pointer;
}
```

> 注：`.char-dot-more` 的 `openVariantPanel` 方法在 Task 4 实现，Task 1 结束前点它无害（方法未定义会在 console 报错，暂不验证该交互）。

- [ ] **Step 5: 验证（4.1 html-editor 自闭环 + 4.2 verify）**

- 起 dev server（项目根）：`python 符号/dev_server.py`（connect 探测 52330→52331）。
- 浏览器开 `http://localhost:52331/符号/符号.html`，点左侧树 `人/职业`。
- 期望：主网格 60 张卡（原 348 成员）；"共 60 个"；男厨师卡显示中性版 👨🍳 + 5 个肤色点。
- 点某肤色点 → 右侧详情显示该肤色变体。
- 点 `标志/旗帜/国旗` → 259 旗帜平铺不折叠（无肤色变体）。

---

### Task 2: 概览分段折叠

**Files:**
- Modify: `符号/符号.js` — `overviewSegments` + `viewItems`/`viewFirstItem` + `onOverviewLocalPageChange`；删 `previewItems`
- Modify: `符号/符号.html` — 概览分段模板（`seg.preview` → 卡片渲染）

**Interfaces:**
- Consumes: Task 1 的 `groupSeqsByBase`/`cardWindow`/`cardsToMembers`/`makeCard`；既有 `overviewSegments`/`viewItems`/`viewFirstItem`/`onOverviewLocalPageChange`/`collectNodeMembers`/`mergeRanges`/`PREVIEW_N`
- Produces: `overviewSegments` 各段 `count`/`preview`/`pageCount` 改为卡片语义；`viewItems()` 返回平铺成员；`viewFirstItem()` 返回首卡 `.main`

- [ ] **Step 1: 符号.js 改 `overviewSegments`**

将 766–794 行 `overviewSegments` 整体替换为：

```js
		/** 概览视图的分段：本级 + 各直接子节点（各段 preview=卡片，count=卡片数；本级按卡分页，子段前 PREVIEW_N 张卡） */
		overviewSegments() {
			const node = this.selectedTag && this.selectedTag.node;
			if (!node) return [];
			const segs = [];
			const ownRanges = mergeRanges(node.ranges || []);
			const ownSeqs = node.seqs || [];
			if (ownRanges.length || ownSeqs.length) {
				const total = rangeCount(ownRanges) + groupSeqsByBase(ownSeqs).size;
				const size = this.gridPageSize;
				const start = (this.overviewLocalPage - 1) * size;
				const preview = cardWindow(ownRanges, ownSeqs, start, size);
				segs.push({ name: '本级', path: this.selectedTag.path, node, count: total, preview, page: this.overviewLocalPage, pageCount: Math.max(1, Math.ceil(total / size)), local: true });
			}
			for (const [name, child] of Object.entries(node.children || {})) {
				const m = collectNodeMembers(child);
				if (!m.ranges.length && !m.seqs.length) continue;
				const count = rangeCount(m.ranges) + groupSeqsByBase(m.seqs).size;
				const preview = cardWindow(m.ranges, m.seqs, 0, PREVIEW_N);
				segs.push({ name, path: this.selectedTag.path + '/' + name, node: child, count, preview, hasMore: preview.length < count });
			}
			return segs;
		},
```

删除 `previewItems`（947–955 行，Task 2 后无调用方）。

- [ ] **Step 2: 符号.js 改 `viewItems` / `viewFirstItem` / `onOverviewLocalPageChange`**

`viewItems`（934 行）与 `viewFirstItem`（939 行）替换为：

```js
		/** 当前视图全部成员（概览=各段卡片平铺；完整=当前页卡片平铺；渲染检测/自动选中用） */
		viewItems() {
			if (this.gridOverview) return cardsToMembers(this.overviewSegments.flatMap(s => s.preview));
			return this.gridItems;
		},
		/** 当前视图首条成员（自动选中用；概览取首段首卡 main） */
		viewFirstItem() {
			if (this.gridOverview) {
				const s = this.overviewSegments[0];
				return (s && s.preview.length) ? s.preview[0].main : null;
			}
			return this.gridItems.length ? this.gridItems[0] : null;
		},
```

`onOverviewLocalPageChange`（971 行）改成员平铺：

```js
		/** 概览本级段翻页：更新页码并刷新渲染能力 */
		onOverviewLocalPageChange(page) {
			this.overviewLocalPage = page;
			const seg = this.overviewSegments.find(s => s.local);
			if (seg && seg.preview.length) this.refreshRenderability(cardsToMembers(seg.preview));
		},
```

- [ ] **Step 3: 符号.html 概览分段模板改卡片渲染**

将 140 行的 `v-for="item in seg.preview"` 卡片块替换为（与 Task 1 主网格卡片结构一致，key 前缀 `'o'` 保留、去拖拽 ✎ 按钮保留）：

```html
										<div class="char" v-for="card in seg.preview" :key="'o' + cellKey(card.main)" :draggable="editEnabled" :class="{ sel: isSelected(card.main), 'has-variants': itemDual(card.main), 'is-ctrl': isControlCode(card.main) }" :title="itemTitle(card.main)" @click="selectItem(card.main)" @dragstart="onDragStart(card.main, $event)" @dragend="onDragEnd">
											<button v-if="editEnabled" class="tag-edit-btn" @click.stop="openTagEditor(card.main)" title="移动/添加到其他标签">✎</button>
											<span class="char-symbol" :class="{ tofu: renderState(card.main) === false }">
												<template v-if="renderState(card.main) === true">
													<template v-if="itemDual(card.main)">
														<span class="variant text vs-text" @click.stop="copyGridVariant(card.main, 'text')">{{ variantText(cellText(card.main), 'text') }}</span>
														<span class="variant emoji" @click.stop="copyGridVariant(card.main, 'emoji')">{{ variantText(cellText(card.main), 'emoji') }}</span>
													</template>
													<template v-else>{{ cellText(card.main) }}</template>
												</template>
												<span v-else-if="renderState(card.main) === false" class="tofu-hint">当前字体不支持</span>
												<span v-else class="tofu-pending" aria-hidden="true"></span>
											</span>
											<span class="char-name">{{ gridItemName(card.main) }}</span>
											<div v-if="card.dots.length" class="char-dots" @dragstart.stop.prevent @click.stop>
												<span v-for="d in card.dots.slice(0, 8)" :key="cellKey(d)" class="char-dot" :class="{ sel: isSelected(d) }" :title="itemTitle(d)" @click.stop="selectItem(d)">{{ cellText(d) }}</span>
												<span v-if="card.overflow" class="char-dot-more" title="查看全部变体" @click.stop="openVariantPanel(card)">+{{ card.overflow }}</span>
											</div>
											<span v-if="isControlCode(card.main)" class="ctrl-badge">控制码</span>
										</div>
```

- [ ] **Step 4: 验证（html-editor 自闭环 + verify + testing）**

- 点 `emoji（绘文字）`（非叶子）→ 概览视图。"本级"段共 1453 卡（1184 单码位 + 269 变体组），每页 100 卡分 15 页；分页正常翻页。
- "本级"段内找牵手组（`人牵手人`）→ 主符号 + 8 点 + `+17` 按钮（26 成员）。
- 单成员组（浅肤色节点 209 组中 150 组）无点行，视觉正常。

---

### Task 3: 搜索结果折叠

**Files:**
- Modify: `符号/符号.js` — `searchCardsFromKeys`（新方法）+ `searchSections` + `onSearchSectionPageChange`
- Modify: `符号/符号.html` — 搜索分节模板（`sec.items` → 卡片渲染）

**Interfaces:**
- Consumes: Task 1 的 `variantGroupKey`/`makeCard`/`cardsToMembers`；既有 `searchSections`/`onSearchSectionPageChange`/`memberKey`/`intersectionKeys`/`searchTokens`
- Produces: `searchCardsFromKeys(keys, preferKeys) → card[]`；`searchSections` 各节 `items`（卡片）/`total`（卡片数）/`pageCount`

- [ ] **Step 1: 符号.js 加 `searchCardsFromKeys` 方法**

在 `searchSections` 附近（约 830 行前）新增：

```js
		/** memberKey 集 → 搜索卡片：单码位逐码位 + seqs 按肤色分组折叠；main=搜索命中变体优先（preferKeys），无则无肤色优先，再无则第一个 */
		searchCardsFromKeys(keys, preferKeys) {
			const singles = [];
			const groups = new Map();
			for (const k of keys) {
				if (typeof k === 'number') { singles.push(k); continue; }
				const cps = k.slice(1).split('-').map(Number);
				const gk = variantGroupKey(cps);
				if (!groups.has(gk)) groups.set(gk, []);
				groups.get(gk).push(cps);
			}
			const cards = singles.map(cp => ({ main: cp, dots: [], overflow: 0 }));
			for (const members of groups.values()) {
				if (members.length === 1) { cards.push({ main: members[0], dots: [], overflow: 0 }); continue; }
				let main;
				if (preferKeys && preferKeys.size) {
					main = members.find(cps => preferKeys.has('s' + cps.join('-')));
				}
				if (main === undefined) main = members.find(cps => !cps.some(c => SKIN_CPS.has(c)));
				if (main === undefined) main = members[0];
				const dots = members.filter(cps => cps !== main);
				cards.push({ main, dots, overflow: Math.max(0, dots.length - 8) });
			}
			return cards;
		},
```

- [ ] **Step 2: 符号.js 改 `searchSections`**

`searchSections`（830 行）的 `addSection` 改卡片 + 传 preferKeys：

```js
		/** 字符结果分节：第一节=交集（≥2 token 时），其后每 token 一节；每节独立分页、按卡折叠 */
		searchSections() {
			const results = this.tokenResults;
			const sections = [];
			const pageSize = this.gridPageSize;
			const pages = this.searchSectionPages;
			const addSection = (key, title, keys, emptyHint, preferKeys) => {
				const cards = this.searchCardsFromKeys(keys, preferKeys);
				const total = cards.length;
				const page = pages[key] || 1;
				const start = (page - 1) * pageSize;
				sections.push({
					key, title, total, emptyHint, page,
					pageCount: Math.max(1, Math.ceil(total / pageSize)),
					items: cards.slice(start, start + pageSize)
				});
			};
			if (results.length >= 2) addSection('__inter__', '多标签交集', this.intersectionKeys, this.intersectionHint);
			results.forEach((r, i) => addSection('tok' + i, r.token, r.memberSet, '「' + r.token + '」无匹配（标签或字符）', new Set(r.charHits.map(h => memberKey(h.cp)))));
			return sections;
		},
```

- [ ] **Step 3: 符号.js 改 `onSearchSectionPageChange`**

`onSearchSectionPageChange`（977 行）改成员平铺：

```js
		/** 搜索分节翻页：更新该节页码并刷新渲染能力 */
		onSearchSectionPageChange(key, page) {
			this.searchSectionPages[key] = page;
			const sec = this.searchSections.find(s => s.key === key);
			if (sec && sec.items.length) this.refreshRenderability(cardsToMembers(sec.items));
		},
```

- [ ] **Step 4: 符号.html 搜索分节模板改卡片渲染**

将 111 行 `v-for="mc in sec.items"` 卡片块替换为（卡片结构同前；标题用 `itemTitle(card.main)` 替代 `searchTitle(mc)`，语义等价——都含中文名 + U+ 兜底；控制码前置由 itemTitle 内部处理）：

```html
										<div class="char" v-for="card in sec.items" :key="cellKey(card.main)" :draggable="editEnabled" :class="{ sel: isSelected(card.main), 'has-variants': itemDual(card.main), 'is-ctrl': isControlCode(card.main) }" :title="itemTitle(card.main)" @click="selectItem(card.main)" @dragstart="onDragStart(card.main, $event)" @dragend="onDragEnd">
											<span class="char-symbol" :class="{ tofu: renderState(card.main) === false }">
												<template v-if="renderState(card.main) === true">
													<template v-if="itemDual(card.main)">
														<span class="variant text vs-text" @click.stop="copyGridVariant(card.main, 'text')">{{ variantText(cellText(card.main), 'text') }}</span>
														<span class="variant emoji" @click.stop="copyGridVariant(card.main, 'emoji')">{{ variantText(cellText(card.main), 'emoji') }}</span>
													</template>
													<template v-else>{{ cellText(card.main) }}</template>
												</template>
												<span v-else-if="renderState(card.main) === false" class="tofu-hint">当前字体不支持</span>
												<span v-else class="tofu-pending" aria-hidden="true"></span>
											</span>
											<span class="char-name">{{ gridItemName(card.main) }}</span>
											<div v-if="card.dots.length" class="char-dots" @dragstart.stop.prevent @click.stop>
												<span v-for="d in card.dots.slice(0, 8)" :key="cellKey(d)" class="char-dot" :class="{ sel: isSelected(d) }" :title="itemTitle(d)" @click.stop="selectItem(d)">{{ cellText(d) }}</span>
												<span v-if="card.overflow" class="char-dot-more" title="查看全部变体" @click.stop="openVariantPanel(card)">+{{ card.overflow }}</span>
											</div>
											<span v-if="isControlCode(card.main)" class="ctrl-badge">控制码</span>
										</div>
```

- [ ] **Step 5: 验证（html-editor 自闭环 + verify + testing）**

- 搜 `浅肤色厨师` → 卡片主符号直接显示"浅肤色厨师"（非中性版）；点行其余肤色。
- 搜 `厨师` → 男厨师/女厨师/中性厨师各自成卡（性别不叠），各含 5 肤色点。
- 搜 `旗` → 旗帜平铺不折叠。
- 多标签交集节（≥2 token）正常折叠、分页。

---

### Task 4: 溢出 `+N` 面板

**Files:**
- Modify: `符号/符号.js` — data 加 `variantPanelVisible` / `variantPanelDots` / `variantPanelTitle`；加 `openVariantPanel`
- Modify: `符号/符号.html` — 加变体面板 `el-dialog`
- Modify: `符号/符号.css` — 面板网格样式

**Interfaces:**
- Consumes: Task 1–3 的卡片 `dots`（全量）+ `overflow`；既有 `cellText`/`gridItemName`/`itemTitle`/`cellKey`/`selectItem`/`el-dialog`
- Produces: `openVariantPanel(card)`（写入 panel state）；模板 `+{{ card.overflow }}` 触发点

- [ ] **Step 1: 符号.js data 加 panel state**

在 `searchSectionPages`（676 行）后加：

```js
			variantPanelVisible: false,   // 变体溢出面板开关
			variantPanelDots: [],         // 面板列出的全部变体成员
			variantPanelTitle: '',        // 面板标题（主符号名 + 全部变体）
```

- [ ] **Step 2: 符号.js 加 `openVariantPanel` 方法**

在 `viewSegment`（957 行）后加：

```js
		/** 打开变体溢出面板：列出该卡全部变体（含名字），点选进详情 */
		openVariantPanel(card) {
			this.variantPanelDots = card.dots;
			this.variantPanelTitle = (this.gridItemName(card.main) || '变体') + ' 的全部变体';
			this.variantPanelVisible = true;
		},
```

- [ ] **Step 3: 符号.html 加面板 dialog**

在 `meta-editor-dialog`（374 行）`</el-dialog>` 后加：

```html
			<el-dialog :title="variantPanelTitle" v-model="variantPanelVisible" width="50%" class="variant-panel-dialog">
				<div class="variant-panel-grid">
					<div v-for="d in variantPanelDots" :key="cellKey(d)" class="variant-panel-item" :title="itemTitle(d)" @click="selectItem(d); variantPanelVisible = false">
						<span class="variant-panel-char">{{ cellText(d) }}</span>
						<span class="variant-panel-name">{{ gridItemName(d) }}</span>
					</div>
				</div>
			</el-dialog>
```

- [ ] **Step 4: 符号.css 加面板样式**

在 `.char-dot-more` 样式后追加：

```css
	.variant-panel-grid {
		display: grid;
		grid-template-columns: repeat(6, 1fr);
		gap: 8px;
		max-height: 50vh;
		overflow: auto;
	}
	.variant-panel-item {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 4px;
		padding: 8px 4px;
		border: 1px solid #e0e0e0;
		border-radius: 4px;
		cursor: pointer;
	}
	.variant-panel-item:hover {
		border-color: #4a90d9;
		background: #e8f1fb;
	}
	.variant-panel-char {
		font-size: 22px;
		font-family: var(--sym-font-stack);
		line-height: 1.2;
	}
	.variant-panel-name {
		font-size: 11px;
		color: var(--el-text-color-placeholder);
		text-align: center;
		overflow-wrap: anywhere;
	}
```

- [ ] **Step 5: 验证（html-editor 自闭环 + verify + testing）**

- `emoji（绘文字）` 根本级 → 牵手组点 `+17` → 面板弹 25 个肤色变体（中性版为主符号不在面板）→ 点选某变体 → 详情显示该变体、面板关闭。
- 点行前 8 点正常选中；`+N` 仅在 overflow>0 时出现。

---

### Task 5: 全量回归 + 缓存版本号

**Files:**
- Modify: `符号/符号.html` — `符号.js?v=260833→260840`、`符号.css?v=260836→260840`

**Interfaces:**
- Consumes: Task 1–4 全部改动

- [ ] **Step 1: 更新缓存版本号**

`符号.html` 两处：`<link href="符号.css?v=260836"` → `符号.css?v=260840`；`<script src="符号.js?v=260833">` → `符号.js?v=260840`。

- [ ] **Step 2: 全量回归（testing Playwright）**

1. `人/职业` 60 卡、肤色点选中、详情正确（Task 1 回归）。
2. `emoji（绘文字）` 概览分段折叠 + 本级分页 15 页 + `+17` 面板（Task 2/4 回归）。
3. 搜 `浅肤色厨师` / `厨师` / `旗` 折叠正确（Task 3 回归）。
4. **不折叠回归**：`标志/旗帜/国旗` 259 旗帜平铺；`数字/汉字数字` 单码位平铺；`emoji-text双模` 子标签。
5. 双模字符（♥）主符号仍双变体渲染。
6. 详情区：选中肤色变体 → 所属标签 chips、HTML 转义长文本换行、效果预览/字体切换忠实渲染（回归 v1.12.0 行为）。
7. 拖拽打标/✎ 编辑主符号 → 操作记录面板记录正确（编辑可用需 dev server 在跑）。
8. 控制台无报错（尤其 `openVariantPanel` 未定义类错误已消除）。

- [ ] **Step 3: 静态检查（verify）**

- 语法/AST 平衡：符号.js / 符号.html / 符号.css。
- 确认 `previewItems` 已删除且无残留引用；`gridItems` 仍被既有消费方引用（`viewItems`/`onGridPageChange`/`refreshAfterOp`/模板空态已改 `gridCards`）。

---

## 自审

**1. Spec 覆盖：**
- 折叠分组（去肤色、性别不叠）→ Task 1 helper ✓
- 主符号无肤色优先 / 无则第一个 → `makeCard` ✓
- 点行 ≤8 + overflow +N 面板 → Task 1 模板 + Task 4 ✓
- 三处生效（网格/概览/搜索）+ 搜索命中变体优先 → Task 1/2/3 ✓
- 分页/计数按卡 → `selectedCount`/`gridPageCount`/`overviewSegments`/`searchSections` ✓
- 单成员组无点行 → `makeCard`（length===1 → dots:[]）✓
- 数据零改动 → 全程未 touch 标签.json ✓
- 详情区不动 → `selectItem(main/d)` 复用既有路径 ✓
- 拖拽拖主符号 / 点行不可拖 → `@dragstart.stop.prevent` + `onDragStart(card.main)` ✓

**2. 占位符扫描：** 无 TBD/TODO；所有代码块为完整实现。

**3. 类型/命名一致性：** `{main, dots, overflow}` 三处模板统一；`cardsToMembers` 签名一致；`groupSeqsByBase` 返回 Map，消费方 `.values()`/`.size` 匹配；`cardWindow(ranges, seqs, start, size)` 调用点（gridCards / overviewSegments 两处）参数一致。`variantGroupKey` 输入纯码位数组，`searchCardsFromKeys` 内 `cps` 来自 `k.slice(1).split('-').map(Number)` 与之匹配。
