进度：已完成
创建：20260828
验收标准：对有肤色变体的折叠卡做 ✎ 打标 / 拖拽移动 / 取消打标时，主符号与所有肤色变体一体操作。

实施（2026-08-28）
	折叠卡（有肤色变体）的 ✎ 打标 / 拖拽移动 / 取消打标时，主符号 + 所有肤色变体整组一体操作（变体跟随）。

协议
	整组 payload.cps 传对象数组成员列表 [{cps,zh,en}, ...]（每成员带名字）；op 记 1 条，opChar / cpsHex 显示主符号。

随带修复
	服务器 _add_member 识别「数组的数组」逐个处理（防坏数据）+ seqs_add 防御性排序（遇 dict 不崩）
	前端 cellText / variantGroupKey / groupSeqsByBase 嵌套数据防御（防 NaN）
	men / women / people wrestling 与 with bunny ears 命名区分（摔跤男人 / 女人 / 人，原全叫「摔跤的人」）

落地记录
	commit 10c9476（2026-08-28）符号页：折叠卡整组编辑 + 数据命名修复（v1.14.0）
	本文档是这段内容的唯一文档记录，没有更早的落地记录可指
