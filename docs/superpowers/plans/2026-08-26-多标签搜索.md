# 多标签搜索 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 符号页搜索框支持空格分隔多关键词，先展示多标签交集结果，再逐关键词展示各自结果。

**Architecture:** 把现有 `matchedTags`/`matchedChars` 两个「整串单关键词」computed 拆成 per-token 处理：`matchTagsForToken` + `matchCharsForToken`（提取自现有逻辑），每个 token 算出自己的字符集（标签成员并集优先，字符命中回退），多 token 做集合交集；结果用 `searchSections` computed 组织成分节数组（交集节 + 每 token 节，各节独立分页），模板改为 v-for 渲染分节。

**Tech Stack:** Vue 3 global build + Element Plus（`符号.js` / `符号.html` / `符号.css`），无构建工具，纯静态。

## Global Constraints

- 项目根 `d:\工具兽\静态页面工具`，页面入口 `符号/符号.html`，逻辑全在 `符号/符号.js`
- 单关键词未命中标签时行为与现状一致；命中标签时显示标签成员（有意语义，2026-08-26 裁决确认 tag-first）
- 无 package.json / node_modules；不引 npm 包；不动 `lib/`
- 编码：Tab 缩进、LF、类名语义化、不写「我刚才干了什么」注释
- 验证：dev_server 优先 52330 / 52331 端口，无响应再 `python 符号/dev_server.py`（自动选端口，读输出）

---
### Task 1: JS 数据层（新增，新旧并存）

**Files:**
- Modify: `符号/符号.js` — 顶层 helper + methods + computed + data + watch（纯新增，不改旧逻辑）

**Interfaces:**
- Produces: `memberKey(member)`（顶层）、methods `matchTagsForToken(token)` / `memberSetOfTags(tags)` / `matchCharsForToken(token)` / `computeTokenResult(token)` / `mcFromKey(key)` / `onSearchSectionPageChange(key, page)`、computed `searchTokens` / `tokenResults` / `searchTagGroups` / `intersectionKeys` / `intersectionHint` / `searchSections`、data `searchSectionPages`。Task 2 的模板和 watch 依赖这些名字。

- [ ] **Step 1: 顶层加 memberKey helper（在 `collectNodeMembers` 函数后，L82 之后）**

```js
/** 成员 Set 键：单码位=码位数字；旗序列='sX-Y'（字符串，与数字不撞型） */
function memberKey(member) {
	return Array.isArray(member) ? 's' + member[0] + '-' + member[1] : member;
}
```

- [ ] **Step 2: methods 区加 6 个新方法（`onSearchPageChange` L932 处就地替换为 `onSearchSectionPageChange`；match 系列放 `searchTitle` L1026 前）**

替换 L932-935：
```js
		/** 搜索分节翻页：更新该节页码并刷新渲染能力 */
		onSearchSectionPageChange(key, page) {
			this.searchSectionPages[key] = page;
			const sec = this.searchSections.find(s => s.key === key);
			if (sec && sec.items.length) this.refreshRenderability(sec.items.map(mc => mc.cp));
		},
```

在 `searchTitle`（L1026）前新增：
```js
		/** 关键词命中的标签（name/path/intro/alias 子串匹配，最多 100；逻辑同旧 matchedTags，抽成 per-token） */
		matchTagsForToken(token) {
			const q = token.toLowerCase();
			const out = [];
			for (const t of FLAT) {
				if (t.name.toLowerCase().includes(q) || t.path.toLowerCase().includes(q) || (t.node.intro && t.node.intro.toLowerCase().includes(q)) || (t.node.alias && t.node.alias.some(a => a.toLowerCase().includes(q)))) {
					out.push(t);
					if (out.length >= 100) break;
				}
			}
			return out;
		},
		/** 一组标签的成员并集 → Set<memberKey>（ranges 物化为码位，seqs 物化为 'sX-Y' 键） */
		memberSetOfTags(tags) {
			const set = new Set();
			for (const t of tags) {
				const m = collectNodeMembers(t.node);
				for (const [lo, hi] of m.ranges) for (let cp = lo; cp <= hi; cp++) set.add(cp);
				for (const s of m.seqs) set.add(memberKey(s));
			}
			return set;
		},
		/** 关键词命中的符号：逻辑与旧 matchedChars 一致，抽成 per-token（码点/单字符/名匹配/中文名/旗序列） */
		matchCharsForToken(token) {
			const q = token.toLowerCase();
			const out = [];
			const seen = new Set();
			const cpq = parseCodePointQuery(q);
			if (cpq) {
				const cp = cpq.cp;
				out.push({ char: String.fromCodePoint(cp), cp, zhName: zhNameOf(cp), officialName: nameOf(cp) });
				seen.add(cp);
			}
			const single = Array.from(token);
			if (single.length === 1) {
				const cp = single[0].codePointAt(0);
				out.push({ char: single[0], cp, zhName: zhNameOf(cp), officialName: nameOf(cp) });
				seen.add(cp);
			} else {
				for (const [char, meta] of SYMBOL_MAP) {
					if (char === q) {
						const cp = char.codePointAt(0);
						out.push({ char, cp, zhName: zhNameOf(cp), officialName: nameOf(cp) });
						seen.add(cp);
						break;
					}
				}
			}
			for (const [char, meta] of SYMBOL_MAP) {
				const hit = meta.names.some(n => n.toLowerCase().includes(q))
					|| meta.enames.some(e => e.toLowerCase().includes(q))
					|| meta.aliases.some(a => a.toLowerCase().includes(q));
				if (!hit) continue;
				const cp = char.codePointAt(0);
				if (seen.has(cp)) continue;
				seen.add(cp);
				out.push({ char, cp, zhName: zhNameOf(cp), officialName: nameOf(cp) });
			}
			if (ZH_NAMES) {
				for (const [cp, zh] of ZH_NAMES.names) {
					if (seen.has(cp)) continue;
					if (zh.toLowerCase().includes(q)) {
						seen.add(cp);
						out.push({ char: String.fromCodePoint(cp), cp, zhName: zh, officialName: nameOf(cp) });
					}
				}
			}
			for (const t of FLAT) {
				if (!t.node.seqs) continue;
				for (const s of t.node.seqs) {
					if (!String(s[2] || '').toLowerCase().includes(q) && !String(s[3] || '').toLowerCase().includes(q)) continue;
					const key = 'seq:' + s[0] + '-' + s[1];
					if (seen.has(key)) continue;
					seen.add(key);
					out.push({ char: String.fromCodePoint(s[0], s[1]), cp: [s[0], s[1]], zhName: s[2] || '', officialName: s[3] || '' });
				}
			}
			return out;
		},
		/** 单个 token 结果：标签命中优先（字符集=命中标签成员并集），否则回退字符命中 */
		computeTokenResult(token) {
			const tagHits = this.matchTagsForToken(token);
			if (tagHits.length) {
				return { token, tagHits, memberSet: this.memberSetOfTags(tagHits) };
			}
			const charHits = this.matchCharsForToken(token);
			const memberSet = new Set();
			for (const hit of charHits) memberSet.add(memberKey(hit.cp));
			return { token, tagHits: [], charHits, memberSet };
		},
		/** memberKey → 网格条目对象 {char, cp, zhName, officialName}（供分节网格渲染） */
		mcFromKey(key) {
			if (typeof key === 'number') {
				return { char: String.fromCodePoint(key), cp: key, zhName: zhNameOf(key), officialName: nameOf(key) };
			}
			const [cp1, cp2] = key.slice(1).split('-').map(Number);
			const meta = SEQ_INDEX.get(cp1 + '-' + cp2) || {};
			return { char: String.fromCodePoint(cp1, cp2), cp: [cp1, cp2], zhName: meta.zh || '', officialName: meta.en || '' };
		},
```

- [ ] **Step 3: computed 区新增 6 个（插在 `isSearching` L676 之后、`matchedTags` 之前）**

```js
		/** 搜索词拆分为 token（空白分隔，去空） */
		searchTokens() {
			return this.searchQuery.trim().split(/\s+/).filter(Boolean);
		},
		/** 每个 token 的搜索结果（标签命中 + 字符集） */
		tokenResults() {
			return this.searchTokens.map(t => this.computeTokenResult(t));
		},
		/** 标签匹配：按 token 分组（仅保留有标签命中的 token） */
		searchTagGroups() {
			return this.tokenResults.filter(r => r.tagHits.length).map(r => ({ token: r.token, tagHits: r.tagHits }));
		},
		/** 多 token 交集（Set<memberKey>）；正向 token 不足 2 个返回 null */
		intersectionKeys() {
			const results = this.tokenResults;
			if (results.length < 2) return null;
			let inter = null;
			for (const r of results) {
				if (!r.memberSet.size) return new Set();
				if (inter === null) inter = new Set(r.memberSet);
				else for (const k of inter) if (!r.memberSet.has(k)) inter.delete(k);
			}
			return inter || new Set();
		},
		/** 交集空提示：指出无匹配的 token */
		intersectionHint() {
			const empty = this.tokenResults.filter(r => !r.memberSet.size).map(r => '「' + r.token + '」');
			const base = '没有同时满足所有关键词的字符';
			return empty.length ? base + '（' + empty.join('、') + ' 无匹配）' : base;
		},
		/** 字符结果分节：第一节=交集（≥2 token 时），其后每 token 一节；每节独立分页 */
		searchSections() {
			const results = this.tokenResults;
			const sections = [];
			const pageSize = this.gridPageSize;
			const pages = this.searchSectionPages;
			const addSection = (key, title, keys, emptyHint) => {
				const total = keys.size;
				const page = pages[key] || 1;
				const start = (page - 1) * pageSize;
				sections.push({
					key, title, total, emptyHint, page,
					pageCount: Math.max(1, Math.ceil(total / pageSize)),
					items: [...keys].slice(start, start + pageSize).map(k => this.mcFromKey(k))
				});
			};
			if (results.length >= 2) addSection('__inter__', '多标签交集', this.intersectionKeys, this.intersectionHint);
			results.forEach((r, i) => addSection('tok' + i, r.token, r.memberSet, '「' + r.token + '」无匹配（标签或字符）'));
			return sections;
		},
```

- [ ] **Step 4: data 加 searchSectionPages（L615 `searchPage` 后新增一行，保留 searchPage）**

```js
			searchPage: 1, // 搜索视图页码（Task 2 清理）
			searchSectionPages: {}, // 搜索分节页码（分节 key → 页码）
```

（Task 1 必须新旧并存：`searchPage` 不能删，旧 `searchChars`/`searchPageCount` 仍依赖它，删了旧搜索网格会变空。）

- [ ] **Step 5: watch.searchQuery 重置分页（L1990，两个都重置）**

`this.searchPage = 1;` →
```js
			this.searchSectionPages = {}; // 新搜索 → 所有分节回到第 1 页
			this.searchPage = 1; // 旧逻辑仍依赖，Task 2 清理
```

- [ ] **Step 6: 静态检查**

Run: 逐行审视 diff —— 检查拼写、括号匹配、分号、方法名一致（`matchTagsForToken`/`memberSetOfTags`/`matchCharsForToken`/`computeTokenResult`/`mcFromKey`/`onSearchSectionPageChange`/`searchSections` 前后一致）。确认无旧逻辑被改动。
Expected: 无语法错误。说「已自检」。

- [ ] **Step 7: 页面仍可用（旧逻辑未受影响）**

Run: 确认 dev server（52330 → 52331 → 启动 `python 符号/dev_server.py` 读端口）。Playwright 打开 `http://localhost:PORT/符号/符号.html`，等加载完成，检查 console 无 error，输入单关键词（如「苹果」）确认旧搜索仍工作。
Expected: 页面加载无报错，旧搜索正常（Task 1 是纯新增）。

---
### Task 2: 模板切分节 + 清理旧逻辑 + CSS

**Files:**
- Modify: `符号/符号.html` L91-121 搜索视图块
- Modify: `符号/符号.css` 搜索视图区（`.category-name` 规则后加分组样式）
- Modify: `符号/符号.js` — 删除旧 computed/method/watch

**Interfaces:**
- Consumes: Task 1 的 `searchTagGroups`、`searchSections`、`searchTokens`、`onSearchSectionPageChange`
- Produces: 搜索视图渲染分节；删掉 `matchedTags` / `matchedChars` / `searchChars` / `searchPageCount` / `searchPage` / `onSearchPageChange` / `watch.searchChars`

- [ ] **Step 1: 替换 符号.html L91-121 搜索视图块**

把整个 `<div v-if="isSearching" class="search-view">...</div>`（L91-121）替换为：
```html
							<!-- 搜索视图 -->
							<div v-if="isSearching" class="search-view">
								<!-- 标签匹配：按 token 分组 -->
								<template v-if="searchTagGroups.length">
									<h3 class="category-name">标签匹配</h3>
									<div class="tag-group" v-for="g in searchTagGroups" :key="g.token">
										<h4 class="tag-group-head">{{ g.token }}（{{ g.tagHits.length }}）</h4>
										<div class="matched-tags">
											<div class="tag-match" v-for="t in g.tagHits" :key="t.path" @click="searchSelectTag(t)">
												<span class="tag-chip">{{ t.name }}<span class="path">{{ t.path }}</span></span>
												<div class="tag-alias" v-if="t.node && t.node.alias && t.node.alias.length">别名：{{ t.node.alias.join('、') }}</div>
												<div class="tag-intro" v-if="t.node && t.node.intro">{{ t.node.intro }}</div>
											</div>
										</div>
									</div>
								</template>
								<div class="none" v-else>无匹配标签</div>
								<!-- 字符结果：交集节 + 每 token 一节，各节独立分页 -->
								<template v-for="sec in searchSections" :key="sec.key">
									<h3 class="category-name">{{ sec.title }}（{{ sec.total }}）</h3>
									<div class="char-grid" v-if="sec.items.length">
										<div class="char" v-for="mc in sec.items" :key="cellKey(mc.cp)" :draggable="editEnabled" :class="{ sel: isSelected(mc.cp), 'has-variants': itemDual(mc.cp), 'is-ctrl': isControlCode(mc.cp) }" :title="searchTitle(mc)" @click="selectItem(mc.cp)" @dragstart="onDragStart(mc.cp, $event)" @dragend="onDragEnd">
											<span class="char-symbol" :class="{ tofu: renderState(mc.cp) === false }">
												<template v-if="renderState(mc.cp) === true">
													<template v-if="itemDual(mc.cp)">
														<span class="variant text vs-text" @click.stop="copyGridVariant(mc.cp, 'text')">{{ variantText(cellText(mc.cp), 'text') }}</span>
														<span class="variant emoji" @click.stop="copyGridVariant(mc.cp, 'emoji')">{{ variantText(cellText(mc.cp), 'emoji') }}</span>
													</template>
													<template v-else>{{ cellText(mc.cp) }}</template>
												</template>
												<span v-else-if="renderState(mc.cp) === false" class="tofu-hint">当前字体不支持</span>
												<span v-else class="tofu-pending" aria-hidden="true"></span>
											</span>
											<span class="char-name">{{ mc.zhName || mc.officialName || '' }}</span>
											<span v-if="isControlCode(mc.cp)" class="ctrl-badge">控制码</span>
										</div>
									</div>
									<el-pagination v-if="sec.pageCount > 1" class="grid-pager" layout="prev, pager, next" :total="sec.total" :page-size="gridPageSize" :current-page="sec.page" @current-change="(p) => onSearchSectionPageChange(sec.key, p)"></el-pagination>
									<div class="none" v-if="sec.total === 0">{{ sec.emptyHint }}</div>
								</template>
							</div>
```

- [ ] **Step 2: 删 符号.js 旧搜索逻辑**

删除以下（仅这些，不动其他）：
- computed `matchedTags`（L726-737）
- computed `matchedChars`（L739-800）
- computed `searchChars`（L802-806）
- computed `searchPageCount`（L808-810）
- data `searchPage`（L615；watch.searchQuery 里的 `this.searchPage = 1;` 一并删）
- method `onSearchPageChange`（L932-935，已在 Task 1 Step 2 替换，确认无残留）
- `watch.searchChars`（L2006-2009）

`watch.searchChars` 整块删除后替换为（放原位置）：
```js
			// 搜索关键词变化 → 预检测渲染能力（未检测字符置占位，不闪豆腐块）
			searchTokens(nv) {
				const sec = this.searchSections;
				for (const s of sec) if (s.items.length) this.refreshRenderability(s.items.map(mc => mc.cp));
			}
```

- [ ] **Step 3: CSS 加分组样式（符号.css，`.search-view .category-name` 规则块后）**

```css
.search-view .tag-group {
	margin-bottom: 1.5rem;
}

.search-view .tag-group-head {
	font-size: 1.05rem;
	font-weight: 600;
	margin: 0 0 0.5rem;
}
```

- [ ] **Step 4: 静态检查**

Run: 逐行审视 diff —— 旧逻辑引用是否清干净（grep `matchedTags|matchedChars|searchChars|searchPageCount|searchPage|onSearchPageChange` 应只剩新名字）；模板 `{{`/`}}` 配对；`.searchSections`/`.searchTagGroups` 方法名拼写。
Expected: 无残留旧引用；无语法错误。说「已自检」。

- [ ] **Step 5: 功能验证（多标签核心场景）**

Run: Playwright 打开搜索页，逐场景验证：
1. 输 `蔬菜 绿色`（两标签）→ 第一节「多标签交集」有字符；第二节「蔬菜」、第三节「绿色」各显成员；标签匹配区分两组
2. 输 `蔬菜 不存在的标签` → 交集节空并提示「没有同时满足所有关键词的字符（「不存在的标签」无匹配）」；「不存在的标签」节显示无匹配提示
3. 输单关键词 `苹果` → 无交集节，仅一节，与旧行为等效
4. console 无 error
Expected: 场景 1-4 全部符合。若失败回 Step 1-4 修复后重验。

---
### Task 3: 边界与回归验证

**Files:**
- No code change expected（除非验证发现 bug）
- Modify: `符号/todo.txt` — 功能4 打 ✓

- [ ] **Step 1: 单关键词回归**

Run: 搜索 `1F600`（码点）、`😀`（单字符）、`逗号`（中文名）、`🍌`（emoji 名）——各应正常出结果、点选字符进详情、右侧详情正常。
Expected: 与改动前行为一致（尤其 watch.searchQuery 单字符/码点自动选详情仍生效）。

- [ ] **Step 2: 旗序列（双码位）交集**

Run: 找一个有 seqs 的标签（如旗子/旗帜类）与另一个标签做交集，确认旗序列条目能出现在交集节与 token 节，点选进详情正常（`selectFlag` 路径）。
Expected: 旗序列条目正常渲染与选中。

- [ ] **Step 3: 分页与渲染能力**

Run: 交集节或 token 节字符数 > 500 时，分页控件出现且翻页正常；翻页后新页字符无豆腐块闪烁（renderability 检测生效）。
Expected: 每节独立分页，翻页正常，无 console error。

- [ ] **Step 4: 性能抽查**

Run: 搜两个大标签（如「汉字」+ 某大类），确认输入到出结果无明显卡顿（< 1s）。
Expected: 无感或轻微延迟。

- [ ] **Step 5: 更新 todo.txt**

把 `4，多标签搜索。` 改为 `✓ 4，多标签搜索。`

- [ ] **Step 6: 收尾验证**

Run: 刷新页面重走 Step 1-4 抽查一遍，确认全部通过。
Expected: 全部通过，说「已自检」。
